# คู่มือการระบบนำเข้าและโครงสร้างงบกระทรวง (HosFin MDB Importer Guide)

คู่มือนี้จัดทำขึ้นเพื่ออธิบายหลักการทำงานของระบบนำเข้าข้อมูลฐานข้อมูลกระทรวงสาธารณสุข (.mdb บีบอัดในไฟล์ .zip) และรูปแบบโครงสร้างข้อมูลของแต่ละไฟล์ (C, D, M, Q) เพื่อใช้เป็นแนวทางในการพัฒนาระบบประมวลผลและ Monitor สถานะทางการเงินในอนาคต

---

## 1. ภาพรวมระบบนำเข้าปัจจุบัน (Current Importer System)

ระบบนำเข้าข้อมูลที่ใช้งานในปัจจุบันถูกนำไปผูกไว้กับหน้า **จัดการงบทดลอง (Trial Balance)** โดยมีลักษณะการทำงานหลักดังนี้:

* **รองรับเฉพาะไฟล์บีบอัดที่มีชื่อขึ้นต้นด้วยตัวอักษร D เท่านั้น (เช่น `D1625_xxxx.zip`):** 
  * ระบบดักเช็กทั้งในฝั่งหน้าเว็บ (JavaScript) และฝั่งหลังบ้าน (PHP Controller) เพื่อความถูกต้องของข้อมูล
* **การสกัดไฟล์อัตโนมัติ (Auto-Extraction):**
  * เมื่อผู้ใช้อัปโหลดไฟล์ `.zip` เข้ามา ระบบจะแกะหาไฟล์ที่มีนามสกุล `.mdb` ภายในซิปนั้นโดยอัตโนมัติ (ไม่ว่าไฟล์ด้านในจะตั้งชื่ออะไร) เพื่อส่งต่อให้สคริปต์ Python
* **วิเคราะห์รอบบัญชี (Analyze Period):**
  * สคริปต์ Python ทำการอ่านตาราง `DataIn` เพื่อสรุปช่วงรอบเวลาบัญชีภาษาไทย (เช่น "ตุลาคม 2568") และจำนวนรายการทั้งหมดส่งกลับมาแสดงผลเป็นตารางในหน้าต่าง Modal
* **นำเข้าและเขียนทับแบบทีละเดือน (Period-by-period Inline Import):**
  * ผู้ใช้สามารถคลิกนำเข้าข้อมูลทีละงวดบัญชีได้อย่างราบรื่นในคราวเดียว โดยระบบจะล็อกปุ่มและเปลี่ยนเป็นสถานะ **"กำลังนำเข้า..."** พร้อมไอคอนหมุนโหลด 
  * เมื่อนำเข้าสำเร็จ ปุ่มจะเปลี่ยนเป็น **"นำเข้าแล้ว"** (สีเทาและกดซ้ำไม่ได้)
  * เมื่อกดปิดหน้าต่าง Modal หน้าเว็บจะทำการ Refresh ตัวเองอัตโนมัติเพื่อแสดงกราฟและตัวเลขใหม่เป็นปัจจุบันที่สุด

---

## 2. โครงสร้างไฟล์ข้อมูลของกระทรวงทั้ง 4 ประเภท

จากการวิเคราะห์ระบบนำส่งงบการเงินและต้นทุนของกระทรวง ไฟล์หลักที่ดาวน์โหลดหรือส่งกระทรวงจะประกอบด้วย 4 รูปแบบหลัก โดยทั้งหมด **แชร์ตารางข้อมูลดิบงบทดลอง `DataIn` ที่มีโครงสร้างเหมือนกัน** แต่แตกต่างกันที่รายละเอียดตารางส่วนต่อขยายอื่น ๆ:

```
                  [ งบกระทรวงสาธารณสุข ]
                            │
        ┌──────────────┬────┴─────────┬──────────────┐
        ▼              ▼              ▼              ▼
   [ C1625 ]      [ D1625 ]      [ M1625 ]      [ Q1625 ]
   ตรวจคุณภาพ     งบทดลองหลัก     ติดตามแผน MOC    ต้นทุน QM
  (QA Check)     (Trial Balance)   (Planfin)     (Costing)
```

### 1) 🟩 ไฟล์ D1625.mdb (ข้อมูลบัญชีหน่วยงาน - ที่ใช้งานอยู่ปัจจุบัน)
* **รหัสขึ้นต้น:** `D` (เช่น `D1625_25690820_1341.zip`)
* **บทบาท:** ข้อมูลรายงานงบทดลองจริงรายเดือนของโรงพยาบาล ใช้เก็บยอดคงเหลือเดบิต/เครดิตรายเดือน
* **ตารางสำคัญ:**
  * `DataIn`: ยอดบัญชีจริงรายเดือน (เดบิต/เครดิตยกมา, ยอดระหว่างเดือน, และยอดสุทธิปลายงวด)
  * `AccCode`, `AllCodeTbl`: รหัสผังบัญชีของกระทรวงและรายชื่อหมวดหมู่
  * `AllRatio1Tbl`, `AllRatio2Tbl`: โครงสร้างเกณฑ์การคำนวณและสูตรอัตราส่วนทางการเงิน (Financial Ratio)

### 2) 🟥 ไฟล์ C1625.mdb (ข้อมูลการตรวจคุณภาพงบทดลอง - QA)
* **รหัสขึ้นต้น:** `C` (เช่น `C1625_25690820_2128.zip`)
* **บทบาท:** ใช้เพื่อประเมินความสอดคล้องและความถูกต้องของบัญชีว่างบดุลของ รพ. มีจุดที่ไม่สมดุลหรือติดลบผิดปกติหรือไม่
* **ตารางสำคัญ:**
  * `CheckTbl`, `CheckTbl01`, `CheckTbl03`, `CheckTbl03Dr`, `CheckTbl03Cr`: กฎการจับคู่ตรวจบัญชีและเกณฑ์การผ่านงบของกองเศรษฐกิจสุขภาพ
  * `PointTbl`, `PointTblX`, `PointTblY`: เกณฑ์การหักคะแนนและการคำนวณเกรดคุณภาพของบัญชีโรงพยาบาล

### 3) 🟦 ไฟล์ M1625.mdb (ข้อมูลติดตามแผนเงินบำรุง MOC - Planfin)
* **รหัสขึ้นต้น:** `M` (เช่น `M1625_25690820_1320.zip`)
* **บทบาท:** บันทึกข้อมูลประมาณการแผนรายได้และรายจ่ายเงินบำรุงล่วงหน้าเพื่อใช้ประเมินวินัยทางการเงินและควบคุมความเสี่ยง
* **ตารางสำคัญ:**
  * `AccPlan`, `PlaNFin`, `PlanAdmin`, `PlanTbl`, `PlanTblRatio`: รหัสหมวดหมู่แผนงานและเป้าหมายตัวชี้วัดความเสี่ยง
  * `hig_est_current`, `hig_past`, `org_est_current`: เก็บข้อมูลประมาณการรายรับ/รายจ่ายแต่ละไตรมาสและเดือนที่ รพ. ได้เสนอขออนุมัติไว้

### 4) 🟨 ไฟล์ Q1625.mdb (ข้อมูลต้นทุนบริการหน่วยงาน - Quick Method)
* **รหัสขึ้นต้น:** `Q` (เช่น `Q1625_25690820_2246.zip`)
* **บทบาท:** ใช้สำหรับคำนวณต้นทุนการจัดบริการสุขภาพแบบเร่งด่วนของหน่วยบริการ (Costing analysis) เพื่อนำไปคิดสัดส่วนงบค่าชดเชยต่าง ๆ
* **ตารางสำคัญ:**
  * `CPGID`, `CPG_name`, `MapQM`: ตารางระบุกลุ่มต้นทุน (เช่น ค่าจ้างและสวัสดิการ LC, ค่าใช้สอยและโสหุ้ย CC, ค่าเสื่อมวัสดุ MC) เพื่อจับคู่งบจริงแยกตามส่วนบริการ
  * `DataIn00`, `DataIn00Template`: เก็บตัวเลขสถิติกิจกรรมการให้บริการจริง (เช่น จำนวนผู้รับบริการ OP, IP แยกสิทธิหลักประกันสุขภาพ) ของโรงพยาบาลในแต่ละงวด

---

## 3. สถาปัตยกรรมทางเทคนิคที่รองรับการใช้งานในอนาคต

ระบบหลังบ้านถูกออกแบบไว้รองรับการต่อยอดเป็นโมดูลอื่น ๆ ได้อย่างยืดหยุ่น:

1. **ใช้ Python Pure-Parser (`access-parser`):**
   * ทำงานได้ 100% ทั้งบน Windows และ Linux (ไม่ต้องพึ่งพาระบบ ODBC หรือ Access Engine ที่ติดตั้งยาก) และถูกเพิ่มเข้าในระบบการ Synchronize DB อัตโนมัติ (`Upgrade Structure`) เรียบร้อยแล้ว
2. **ระบบแปลงรหัสภาษาไทยข้ามแพลตฟอร์ม (Cross-Platform Thai Encoding Recovery):**
   * สคริปต์ได้จัดการระบบแปลงรหัสผ่านคำสั่ง `iconv` ในรูปแบบ `iconv('TIS-620', 'UTF-8//IGNORE', $outputStr)` เพื่อรองรับการเปิดใช้งานบน Windows Server ท้องถิ่นของ รพ. ต่าง ๆ และระบบ Linux ได้โดยไม่มีปัญหาตัวอักษรกลายเป็นช่องสี่เหลี่ยมหรือ Malformed UTF-8
3. **ระบบสวมชื่อผังบัญชีสะอาด (Account Name Replacement):**
   * เนื่องจากภาษาไทยจาก Access ในตาราง `DataIn` มักเกิดข้อผิดพลาดหรือมีคีย์ `(Yes)` / `(No)` พ่วงท้ายมาด้วย ระบบหลังบ้านจึงใช้กลไกการสวมทับชื่อผังบัญชีอย่างเป็นทางการจากระบบฐานข้อมูลหลัก `hosfin_dtl_mappings` ทันทีที่อิมพอร์ต เพื่อให้ฐานข้อมูลปลายทางสะอาดสูงสุด

---

## 4. แผนงานขยายผลในอนาคต (Future Roadmap)

เนื่องจากบางฟังก์ชันของระบบสามารถใช้งานและดึงข้อมูลพื้นฐานจากไฟล์ **งบทดลองหลักที่เป็นตัว D** เป็นแกนตั้งต้นได้เลย ในอนาคตจึงสามารถเพิ่มโมดูลวิเคราะห์ข้อมูลอื่น ๆ ได้โดยไม่ต้องรื้อฐานระบบเดิม ดังนี้:

* **การประเมินคะแนนงบการเงินภายในองค์กร (C1625 QA simulation):**
  * เขียนสคริปต์จำลองการรันระบบตรวจสอบโดยนำข้อมูลจริงจากตาราง `DataIn` ของงบไฟล์ D มาคำนวณตามกฎในตารางตรวจสอบคุณภาพของไฟล์ C เพื่อแสดงผลคะแนนและรายชื่อจุดผิดพลาดทางบัญชีให้ทราบล่วงหน้าแบบทันควัน
* **การวิเคราะห์ส่วนต่างแผนงบประมาณ (Plan vs Actual Variance):**
  * นำยอดใช้จริงจากงบทดลองไฟล์ D ไปชนเปรียบเทียบกับตัวเลขแผนประมาณการเงินบำรุงที่ดึงจากไฟล์ M เพื่อคำนวณหาสัดส่วนการใช้งบเกินเป้า และวิเคราะห์สถานะวิกฤตทางการเงินรายสัปดาห์
* **การคำนวณต้นทุนการรักษาพยาบาล (QM Unit Costing):**
  * ทำตัวเช็กสัดส่วนโดยดึงข้อมูลยอดเงินจากงบไฟล์ D ไปจัดหมวดหมู่ต้นทุนตามรหัสในแผนที่ `MapQM` (ดึงจากไฟล์ Q) เพื่อประเมินหาต้นทุนบริการต่อหน่วย (OPD Cost/IPD Cost)

---
*บันทึกปรับปรุงล่าสุด: 20 สิงหาคม 2569 - ฝ่ายพัฒนาระบบ HosFin*
